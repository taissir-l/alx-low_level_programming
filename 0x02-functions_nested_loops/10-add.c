#include "main.h"
/**
 * add - unction that adds two integers and returns the result
 *
 * @i: the first number
 * @j: the second number
 * Return: the value
 */
int add(int i, int j)
{
	int x;

	x = i + j;
	return (x);
}
